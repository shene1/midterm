//! Define the model class Note here
import 'package:flutter/material.dart';

class Note {
  String title;
  String content;
  String description;
  Color noteColor;

  Note(this.title, this.content, this.description, this.noteColor);
}
